export const listNFT = async (marketplaceContract, nftContract, tokenId, price, userWalletAddress) => {
    if(path === '/sell_nft') {
        try{

        }catch(err){
            throw err;
        }
    }

    if(path === '/create_auction') {

    }
}